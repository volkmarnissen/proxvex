{
  "name": "OIDC Authentication",
  "description": "Enable OpenID Connect authentication via Zitadel. Automatically creates an OIDC client in Zitadel and provides credentials as template variables.",
  "tags": ["security", "authentication"],

  "parameters": [
    {
      "id": "oidc_app_name",
      "name": "OIDC Application Name",
      "type": "string",
      "required": false,
      "description": "Name of the OIDC application in Zitadel (defaults to hostname)"
    },
    {
      "id": "OIDC_PROJECT_NAME",
      "name": "Zitadel Project Name",
      "type": "string",
      "required": false,
      "description": "Name of the Zitadel project for this application (defaults to hostname)"
    },
    {
      "id": "oidc_callback_path",
      "name": "Callback Path",
      "type": "string",
      "required": false,
      "default": "/auth/strategy/callback",
      "advanced": true,
      "description": "OIDC callback path appended to the application URL"
    }
  ],

  "dependencies": [
    { "application": "zitadel" }
  ],

  "properties": [],

  "installation": {
    "pre_start": [
      "162-conf-configure-oidc-app.json"
    ],
    "post_start": [
      "350-post-setup-oidc-client.json",
      "355-post-configure-oidc-app.json"
    ]
  },

  "reconfigure": {
    "post_start": [
      "350-post-setup-oidc-client.json",
      "355-post-configure-oidc-app.json"
    ]
  },

  "disable": {
    "pre_start": [
      "162-conf-disable-oidc-app.json"
    ]
  },

  "notes_key": "oidc"
}
