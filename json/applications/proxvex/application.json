{
  "name": "Proxvex",
  "description": "Deploys and manages LXC containers on Proxmox from OCI images via a web UI and API, and lets you easily define custom container services",
  "extends": "oci-image",
  "icon": "favicon.svg",
  "tags": ["infrastructure"],
  "supported_addons": ["addon-oidc"],
  "oidc_roles": [
    { "key": "admin", "display_name": "Administrator", "group": "deployer" }
  ],
  "parameters": [
    {
      "id": "http_port",
      "name": "HTTP Port",
      "type": "string",
      "default": "3080",
      "required": false,
      "advanced": true
    },
    {
      "id": "https_port",
      "name": "HTTPS Port",
      "type": "string",
      "default": "3443",
      "required": false,
      "advanced": true
    }
  ],
  "properties": [
    { "id": "hostname", "value": "proxvex" },
    { "id": "oci_image", "value": "ghcr.io/proxvex/proxvex:{{oci_image_tag}}" },
    { "id": "uid", "value": "1001" },
    { "id": "gid", "value": "1001" },
    { "id": "username", "value": "lxc" },
    { "id": "http_port", "value": "{{http_port}}" },
    { "id": "https_port", "value": "{{https_port}}" },
    { "id": "volume_backup", "value": "true" },
    { "id": "volumes", "value": "config=/config\nsecure=/secure,0700" },
    { "id": "ssl_mode", "value": "native" },
    { "id": "oidc_callback_path", "value": "/api/auth/callback" },
    { "id": "oidc_required_role", "value": "admin" }
  ],
  "check": {
    "check": [
      "check-oidc-endpoint.json"
    ]
  },
  "url": "https://github.com/proxvex/proxvex",
  "documentation": "https://github.com/proxvex/proxvex/blob/main/README.md",
  "source": "https://github.com/proxvex/proxvex",
  "vendor": "Carcam 360"
}