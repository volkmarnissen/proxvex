{
  "name": "zitadel",
  "description": "ZITADEL is an open source identity management platform.",
  "extends": "docker-compose",
  "icon": "icon.png",
  "parameters": [
    {
      "id": "hostname",
      "name": "Hostname",
      "type": "string",
      "required": false,
      "description": "Hostname for the Docker Compose container. Required when creating an application from the docker-compose framework.",
      "template": "application.json",
      "default": "zitadel"
    },
    {
      "id": "ZITADEL_EXTERNALDOMAIN",
      "name": "External Domain",
      "type": "string",
      "required": false,
      "description": "Public domain name for Zitadel (used in URLs and OIDC config)",
      "default": "{{ hostname }}"
    },
    {
      "id": "ZITADEL_EXTERNALSECURE",
      "name": "External Secure",
      "type": "string",
      "required": false,
      "advanced": true,
      "default": "false",
      "description": "Set to 'true' when Zitadel is reached via HTTPS from outside (e.g. behind a TLS reverse proxy)."
    },
    {
      "id": "ZITADEL_EXTERNALPORT",
      "name": "External Port",
      "type": "string",
      "required": false,
      "advanced": true,
      "default": "8080",
      "description": "Public TCP port Zitadel is reached on (e.g. 443 behind nginx, 8080 for direct exposure)."
    },
    {
      "id": "ZITADEL_FORWARDED_PROTO",
      "name": "Forwarded Proto",
      "type": "string",
      "required": false,
      "advanced": true,
      "default": "http",
      "description": "X-Forwarded-Proto value the login UI sends to zitadel-api (http or https). Match the public scheme."
    },
    {
      "id": "ZITADEL_PUBLIC_BASE_URL",
      "name": "Public Base URL",
      "type": "string",
      "required": false,
      "advanced": true,
      "default": "http://{{ ZITADEL_EXTERNALDOMAIN }}:8080",
      "description": "Full public base URL the user types in their browser (no trailing slash). Used in BASEURI/OIDC default URLs. Example: https://auth.example.com (no port for default 443/80)."
    },
    {
      "id": "smtp_own_domain",
      "name": "Own mail domain (configure Cloudflare DNS)",
      "type": "boolean",
      "default": false,
      "description": "When checked, create MX and SPF DNS records in Cloudflare for the mail domain (requires a cloudflare stack with CF_TOKEN). Leave unchecked if using an existing hosted mail setup where DNS already points to the provider."
    },
    {
      "id": "smtp_mail_domain",
      "name": "Mail domain",
      "type": "string",
      "required": false,
      "default": "",
      "if": "smtp_own_domain",
      "description": "Apex domain the mail is sent from (e.g. example.com). Looked up in Cloudflare and used as the zone for MX/SPF records."
    },
    {
      "id": "smtp_mx_target",
      "name": "MX target",
      "type": "string",
      "required": false,
      "default": "",
      "if": "smtp_own_domain",
      "description": "Hostname the MX record points at (e.g. mxext1.mailbox.org). Consult your mail provider's documentation."
    },
    {
      "id": "smtp_spf_value",
      "name": "SPF TXT value",
      "type": "string",
      "required": false,
      "default": "v=spf1 ~all",
      "if": "smtp_own_domain",
      "description": "Full SPF TXT record value, e.g. 'v=spf1 include:mailbox.org ~all'. Consult your mail provider's documentation."
    }
  ],
  "properties": [
    {
      "id": "volumes",
      "default": "bootstrap=bootstrap,0750,1000:1001"
    },
    {
      "id": "compose_file",
      "default": "file:Zitadel.docker-compose.yml"
    },
    {
      "id": "ostype",
      "value": "alpine"
    },
    {
      "id": "ssl_mode",
      "value": "native"
    },
    {
      "id": "https_port",
      "value": "1443"
    },
    {
      "id": "ssl.addon_volumes",
      "value": "certs=certs,0755,0:0\noci_deployer=/etc/lxc-oci-deployer,0755,0:0"
    },
    {
      "id": "ssl.needs_server_cert",
      "value": "true"
    },
    {
      "id": "ssl.needs_ca_cert",
      "value": "true"
    },
    {
      "id": "startup_order",
      "value": "20"
    },
    {
      "id": "startup_up",
      "value": "15"
    },
    {
      "id": "startup_timeout",
      "value": "300"
    },
    {
      "id": "check_file_paths",
      "value": "/bootstrap/login-client.pat\n/bootstrap/admin-client.pat"
    }
  ],
  "dependencies": [
    {
      "application": "postgres"
    }
  ],
  "installation": {
    "post_start": [
      "340-post-setup-deployer-in-zitadel.json",
      "385-post-configure-mail-dns.json",
      "380-post-show-admin-credentials.json"
    ]
  },
  "stacktype": [
    "postgres",
    "oidc",
    "cloudflare"
  ]
}
