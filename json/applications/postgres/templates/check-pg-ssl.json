{
  "execute_on": "ve",
  "name": "Check PG SSL",
  "description": "Checks if PostgreSQL has SSL enabled. Only runs when SSL addon is active.",
  "skip_if_all_missing": ["has_addon_ssl"],
  "commands": [
    {
      "name": "Check PG SSL",
      "script": "host-check-pg-ssl.sh",
      "outputs": ["check_pg_ssl"]
    }
  ]
}
