{
  "name": "Set Reconfigure Defaults",
  "description": "Sets default values for parameters that are required by templates but not needed in reconfigure mode (container already exists).",
  "commands": [
    {
      "properties": [
        {
          "id": "ostype",
          "value": "alpine"
        },
        {
          "id": "template_path",
          "value": "not-needed-for-reconfigure"
        }
      ]
    }
  ]
}
