{
  "name": "Cloudflare",
  "description": "Cloudflare API credentials for DNS-01 ACME challenge (Let's Encrypt). acme.sh dns_cf resolves zones automatically — no Zone ID needed.",
  "variables": [
    { "name": "CF_TOKEN", "external": true }
  ]
}
