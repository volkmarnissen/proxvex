{
  "name": "Disable SSL App Configuration",
  "description": "Application-specific SSL cleanup when the SSL addon is disabled. The default shared script is a no-op. Applications can override conf-disable-ssl-app.sh in their own scripts/ directory.",
  "execute_on": "ve",
  "parameters": [
    {
      "id": "hostname",
      "name": "Hostname",
      "type": "string",
      "required": true,
      "description": "Container hostname"
    }
  ],
  "commands": [
    {
      "name": "Disable SSL App Configuration",
      "script": "conf-disable-ssl-app.sh",
      "outputs": [
        "ssl_app_disabled"
      ]
    }
  ]
}
