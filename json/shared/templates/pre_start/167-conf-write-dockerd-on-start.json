{
  "execute_on": "ve",
  "name": "Write Dockerd On-Start Script",
  "description": "Deploys an on-start hook that launches dockerd on every container start for docker-compose based applications. The hook lives in /etc/proxvex/on_start.d/ and is invoked by the on_start_container dispatcher.",
  "parameters": [
    "hostname",
    "vm_id",
    "docker_registry_mirror",
    "ghcr_registry_mirror"
  ],
  "commands": [
    {
      "script": "conf-write-dockerd-on-start.sh",
      "library": "pve-common.sh",
      "description": "Write dockerd on-start drop-in script to host-side proxvex volume"
    }
  ]
}
