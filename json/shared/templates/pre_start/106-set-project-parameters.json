{
  "name": "Set Project Parameters",
  "description": "Set project-specific default parameters. Override this template in your local directory to set defaults for all applications (e.g. acme_san, oidc_issuer_url, domain_suffix).",
  "parameters": [
    {
      "id": "oidc_issuer_url",
      "name": "OIDC Issuer URL",
      "type": "string",
      "default": "",
      "advanced": true,
      "description": "External OIDC issuer URL (override in local to set project default)"
    }
  ],
  "commands": [
    {
      "properties": {
        "id": "oidc_issuer_url",
        "default": ""
      }
    }
  ]
}
