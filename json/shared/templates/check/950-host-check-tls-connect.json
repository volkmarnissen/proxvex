{
  "execute_on": "ve",
  "name": "Check TLS Connect",
  "description": "Checks TLS connection to a container port.",
  "skip_if_all_missing": [
    "has_addon_ssl",
    "check_tls_port"
  ],
  "parameters": [
    "check_tls_port"
  ],
  "commands": [
    {
      "name": "Check TLS Connect",
      "script": "host-check-tls-connect.sh",
      "outputs": [
        "check_tls_connect"
      ],
      "library": [
        "ve-global.sh"
      ]
    }
  ]
}
