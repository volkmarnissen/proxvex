{
  "execute_on": "ve",
  "name": "Check OIDC API Protected",
  "description": "Checks that the deployer API returns 401 without authentication token.",
  "skip_if_all_missing": [
    "check_oidc_api_protected"
  ],
  "parameters": [
    "check_oidc_api_protected"
  ],
  "commands": [
    {
      "name": "Check OIDC API Protected",
      "script": "host-check-oidc-api-protected.sh",
      "outputs": [
        "check_oidc_api_protected_result"
      ],
      "library": [
        "ve-global.sh"
      ]
    }
  ]
}
