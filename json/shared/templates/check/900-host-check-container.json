{
  "execute_on": "ve",
  "name": "Check Container",
  "description": "Checks container is running and notes contain managed marker.",
  "commands": [
    {
      "name": "Check Container Status",
      "script": "host-check-container.sh",
      "outputs": ["check_results"]
    }
  ]
}
