{
  "execute_on": "ve",
  "name": "Check OIDC Enabled",
  "description": "Checks that OIDC is enabled on the deployer API (/api/auth/config).",
  "skip_if_all_missing": [
    "check_oidc_enabled"
  ],
  "parameters": [
    "check_oidc_enabled"
  ],
  "commands": [
    {
      "name": "Check OIDC Enabled",
      "script": "host-check-oidc-enabled.sh",
      "outputs": [
        "check_oidc_enabled_result"
      ],
      "library": [
        "ve-global.sh"
      ]
    }
  ]
}
