{
  "execute_on": "lxc",
  "name": "Install ACME Cert Renewal",
  "description": "Installs a drop-in script that handles ACME certificate issuance and renewal via acme.sh with Cloudflare DNS on each container start.",
  "skip_if_all_missing": ["CF_TOKEN"],
  "parameters": [
    {
      "id": "CF_TOKEN",
      "name": "Cloudflare API Token",
      "type": "string",
      "required": true,
      "description": "Cloudflare API token with DNS edit permissions"
    },
    {
      "id": "acme_domain",
      "name": "Domain",
      "type": "string",
      "required": true,
      "description": "Fully qualified domain name for the certificate"
    },
    {
      "id": "acme_email",
      "name": "Email",
      "type": "string",
      "default": "",
      "description": "Email for Let's Encrypt registration and notifications"
    },
    {
      "id": "acme.cert_dir",
      "name": "Certificate Directory",
      "type": "string",
      "default": "/etc/ssl/addon",
      "description": "Container directory where certificates are written"
    },
    {
      "id": "acme.needs_server_cert",
      "name": "Needs Server Certificate",
      "type": "string",
      "default": "true",
      "description": "Whether to issue a server certificate (privkey, cert, fullchain)"
    },
    {
      "id": "acme.needs_ca_cert",
      "name": "Needs CA Certificate",
      "type": "string",
      "default": "false",
      "description": "Whether to write the CA/intermediate certificate (chain.pem)"
    },
    {
      "id": "alpine_mirror",
      "name": "Alpine Mirror",
      "type": "string",
      "default": "",
      "advanced": true,
      "description": "Alpine Linux mirror URL for package installation"
    },
    {
      "id": "debian_mirror",
      "name": "Debian Mirror",
      "type": "string",
      "default": "",
      "advanced": true,
      "description": "Debian/Ubuntu mirror URL for package installation"
    }
  ],
  "commands": [
    {
      "script": "post-install-acme-renew-on-start.sh",
      "description": "Create and run ACME renewal on-start drop-in script"
    }
  ]
}
