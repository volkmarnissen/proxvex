{
  "execute_on": "lxc",
  "name": "Start Docker Compose",
  "description": "Start Docker Compose services. Runs 'docker-compose up -d' in the project directory. Docker daemon must be running.",
  "parameters": [
    "vm_id",
    "compose_project",
    "startup_timeout",
    "compose_tag_overrides"
  ],
  "commands": [
    {
      "script": "post-start-docker-compose.sh",
      "library": [
        "versions.sh",
        "versions-local.sh"
      ],
      "description": "Start Docker Compose services with docker-compose up -d"
    }
  ]
}