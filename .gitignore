node_modules/
.DS_Store
._*
*.log

# Build outputs
backend/dist/
frontend/dist/
cli/dist/

# Test coverage
coverage/

# jscpd report output
report/

# Cypress/Playwright outputs
cypress/
playwright-report/
test-results/

# Alpine packaging: ignore generated package directories
alpine/cache/
alpine/package/*/
!alpine/package/apk-template/
!alpine/package/common/
# Keep generator and INIs tracked
!alpine/package/generate-ap.sh
!alpine/package/*.ini
!alpine/package/README.md
# Alpine local repository (build output)
alpine/repo/
#
test-results/
**/secret.txt
**/storagecontext.json
docker/proxvex.tgz

# Environment files with credentials (keep .env.template)
**/.env
!**/.env.template

# E2E test config — contains infrastructure details (host, subnets, IPs).
# Committed as config.json.example; devs copy to config.json locally;
# CI workflows materialize it from the E2E_CONFIG GitHub secret.
e2e/config.json
# Personal Access Tokens
**/*.pat
**/__pycache__/*


**/.unused

# npm lock files (we use pnpm)
package-lock.json
**/package-lock.json
backend/tests/e2e/.nested-vm-ip

# Generated test certificates
e2e/certs/
livetest-diag-*.tar.gz
livetest-results-*/
.livetest-data/
oci*.tgz

# Local hub/spoke dev instances (scripts/dev-hub-spoke-setup.sh)
.dev/
.hubs/

# Rename-script dry-run/apply manifest (reproducible via --dry-run)
scripts/replace_oci_lxc_deployer_changes.json
