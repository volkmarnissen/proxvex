{
  "$id": "categorized-templatelist",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A template list that supports both flat array format and category-based object format",
  "oneOf": [
    { "$ref": "templatelist" },
    {
      "type": "object",
      "properties": {
        "no_extend": {
          "type": "boolean",
          "default": false,
          "description": "If true, do not merge phases from the parent via 'extends'; use only the phases listed on this task. Used by the proxvex migration upgrade-task to avoid inheriting replace_ct from oci-image parent."
        },
        "image": { "$ref": "templatelist" },
        "create_ct": { "$ref": "templatelist" },
        "pre_start": { "$ref": "templatelist" },
        "pre_start_finalize": { "$ref": "templatelist" },
        "start": { "$ref": "templatelist" },
        "post_start": { "$ref": "templatelist" },
        "replace_ct": { "$ref": "templatelist" },
        "check": { "$ref": "templatelist" }
      }
    }
  ]
}
